{
  "name": "pdf-ai-converter-server",
  "version": "1.0.0",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 4000",
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 4000"
  },
  "dependencies": {
    "puppeteer": "^24.30.0"
  }
}
